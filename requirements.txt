# Dependências básicas (todas as plataformas)
opencv-python>=4.5.0
numpy>=1.20.0
pandas>=1.5.0
requests>=2.28.0

# Banco de dados (built-in com Python)
# sqlite3 - já incluído no Python

# Servidor HTTP e visualização
flask>=2.3.0
werkzeug>=2.3.0

# Comunicação em tempo real (WebSockets)
Flask-SocketIO>=5.3.0
python-socketio>=5.7.0
eventlet>=0.33.0

# Dependências específicas do Raspberry Pi
# Instalar apenas quando executar em modo --use-rpi
RPi.GPIO>=0.7.0

# ADS1115 - Conversor ADC I2C para sensores de pressão
adafruit-circuitpython-ads1x15>=2.2.0
adafruit-blinka>=8.0.0

# ⚡ MAX6675 - IMPLEMENTAÇÃO NATIVA! 
# Não precisa mais de bibliotecas externas MAX6675
# O sistema agora usa implementação nativa com RPi.GPIO

# Dependências removidas (não são mais necessárias):
# MAX6675-RPi - Substituído por implementação nativa
# max6675 - Substituído por implementação nativa
# MAX6675 - Substituído por implementação nativa

# Para desenvolvimento (opcionais)
# matplotlib>=3.5.0  # para análises avançadas
# plotly>=5.0.0       # para gráficos interativos